# linkyard

The CLI is **free**. MIT. Clone this repo, run it, keep it.

```bash
python3 -m linkyard --help
```

The old $5 “buy this public repository” SKU is **withdrawn**. Charging for a clone was not a business. The live Payment Link for that SKU is deactivated.

## Optional paid product

**linkyard Sheet — $19 one-time.** A Google Sheet + Apps Script that bulk-creates Stripe Payment Links from spreadsheet rows. The script is **not** in this public repo. Your Stripe restricted key stays in your Google account, not ours.

Fulfillment: after payment, `noxsysops@gmail.com` sends a Drive copy. If the Sheet listing is not live yet, email that address.

Questions: noxsysops@gmail.com
